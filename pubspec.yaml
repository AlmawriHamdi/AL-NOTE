name: al_note
description: Cross-platform handwriting, note-taking, and PDF annotation for students.
publish_to: none
version: 0.0.1+1

environment:
  sdk: 3.12.2
  flutter: 3.44.6

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
